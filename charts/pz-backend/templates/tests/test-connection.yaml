apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pz-backend.fullname" . }}-test-connection"
  labels:
    {{- include "pz-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pz-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
