apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pz-web.fullname" . }}-test-connection"
  labels:
    {{- include "pz-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pz-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
